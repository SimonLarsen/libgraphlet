#ifndef ORCA_GDD_HPP
#define ORCA_GDD_HPP 

#include <vector>
#include <map>
#include <orca/Orca.hpp>

namespace orca {
	void gdd(
		const Orca &orca,
		std::vector<std::map<size_t,float>> &gdd,
		bool normalize = trusize_t,e
	);
}

#endif
